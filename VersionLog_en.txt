0.3.65
- Added: Expanded `PromptTemplates` with channel-role and RPG contract templates (`DiplomacyFallbackRoleTemplate`, `RpgRoleSettingTemplate`, `RpgCompactFormatConstraintTemplate`, `RpgActionReliabilityRuleTemplate`).
- Changed: Hierarchical prompt builder now reads diplomacy fallback role text and RPG role-setting text from templates with placeholder rendering, while preserving old hardcoded fallback behavior.
- Changed: RPG format constraint and action reliability rule now support template-driven rendering, reducing hardcoded prompt text in the builder.
- Changed: Default `SystemPrompt_Default.json` and legacy JSON serializer/parser for `PromptTemplates` are synced with the new fields.
0.3.64
- Added: Introduced `PromptTemplateTextConfig` to externalize shared prompt text sections (`FactGroundingTemplate`, `OutputLanguageTemplate`) into runtime/default config.
- Added: New placeholder renderer `PromptTemplateRenderer` with `{{variable}}` syntax support for reusable prompt templates.
- Changed: Hierarchical prompt builder now renders `fact_grounding` and `output_language` from config templates first, with legacy hardcoded text kept as fallback.
- Changed: Prompt config JSON default template now includes `PromptTemplates` block; legacy serializer/parser and typed codec normalization are both updated for compatibility.
0.3.60
- Changed: Removed bidirectional default-template sync buttons from prompt settings (Apply Default To Runtime / Write Runtime To Default).
- Changed: Removed default-template sync APIs from PromptPersistenceService (GetDefaultTemplatePath, ReloadRuntimeConfigFromDefaultTemplate, SaveRuntimeConfigToDefaultTemplate).
- Updated: Removed related CN/EN localization keys and synced docs (README.md, Api.md, config.md).
0.3.59
- Fixed: Dialogue token parsing now scopes extraction to the response usage object to avoid false matches from assistant text.
- Added: Usage extraction now supports multiple schema aliases (prompt_tokens/input_tokens/promptTokenCount, completion_tokens/output_tokens/candidatesTokenCount, total_tokens/totalTokenCount).
- Added: Automatic fallback to local estimate when provider usage is clearly inconsistent with request/response size; UI keeps the estimated marker.
- Preventive: Added provider-vs-estimate diagnostic logging for faster out.log triage.
0.3.58
- Fixed: Prompt runtime config persistence path moved to user config directory (`Config/RimChat/Prompt/Custom/system_prompt_config.json`) to prevent deploy overwrite loss.
- Added: Automatic migration for legacy prompt config files from old mod/save_data paths on first load.
- Changed: Default-template read now prefers user override file (`Config/RimChat/Prompt/Default/SystemPrompt_Default.json`); write-back now targets this persistent override path.
- Added: `build.ps1` now backs up/restores destination `Prompt/Custom` during deploy as an extra safety net for legacy data.

0.3.57
- Fixed: Strategy follow-up flow now tolerates non-JSON LLM replies by extracting strategy sentences from narrative text and backfilling 3 strategy buttons.
- Improved: Main strategy-missing path also attempts narrative fallback before issuing follow-up requests, reducing repeated `missing/invalid` loops.
- Changed: Strategy follow-up prompt no longer appends full recent dialogue history to reduce roleplay drift and improve structured-output compliance.
- Added: CN/EN fallback reply localization keys (`RimChat_StrategyFallbackReply1/2/3`) for strategy-button recovery flow.

0.3.56
- Fixed: Replaced garbled collapsed-preview hint in prompt settings with keyed localization text (`RimChat_PreviewCollapsedHint`).
- Fixed: Replaced garbled UI literals in prompt editor messages and legacy faction-prompt edit button with localized keys.
- Added: Build-time encoding guard in `build.ps1` to block suspicious mojibake in UI string literals before compilation.
- Improved: Build script now explicitly uses UTF-8 console output to reduce encoding drift during tooling output.

0.3.55
- Added: Prompt settings now include `Apply Default To Runtime`, which reloads runtime prompt config from `Prompt/Default/SystemPrompt_Default.json`.
- Added: Prompt settings now include `Write Runtime To Default`, which writes the current runtime config back to the default template file.
- Added: PromptPersistenceService helper APIs for default-template sync: `GetDefaultTemplatePath`, `ReloadRuntimeConfigFromDefaultTemplate`, `SaveRuntimeConfigToDefaultTemplate`.
- Updated: Added CN/EN localization keys and synced docs (`README.md`, `Api.md`, `config.md`) for the default-template debugging workflow.

0.3.54
- Changed: Removed all `Widgets.DrawBox(rect);` calls across settings UI panels in `RimChat/Config`.
- Visual: Settings pages now render without rectangle stroke outlines that were previously drawn by `DrawBox(rect)`.

0.3.53
- Changed: Removed white border frames from the two main panels in RPG tab (left navigation panel and right editor panel).
- Changed: Removed white border frames from the two main panels in Prompt tab (left navigation panel and right editor panel).

0.3.52
- Changed: "Reset to default" button in AI accordion rows is now visible only when that row is expanded.
- Changed: Removed `>` / `v` arrow symbols from AI accordion row headers.

0.3.51
- Changed: Removed the AI Control quick-search row (search input + Go/Clear buttons) from the top quick-tools header.
- Changed: Removed white border strokes from AI option rows by dropping accordion row outline/bottom separator drawing.
- Visual: Removed the quick-tools summary card white border for a cleaner header style.

0.3.50
- Changed: `MaxAPICallsPerHour` now defaults to `0` (`Unlimited`) and the Security slider supports `0..100` where `0` means no hourly cap.
- Changed: AI Control quick-tools header removed the one-click jump button row (Behavior/Presence/NPC Push/Security).
- Improved: AI quick search now matches both section titles and nested setting items (for example Gift, Aid, Presence, Security options).
- Fixed: RPG Dynamic Injection page no longer duplicates the RimTalk compatibility entry block.
- Fixed: API tab output-language summary no longer repeats the same language text when "Follow system" is enabled.

0.3.45
- Added: A low-visibility `?` hint beside the send button in diplomacy dialogue.
- Added: A hover tooltip that lists NPC potential diplomacy actions with availability status.
- Added: A low-visibility `?` hint beside the send button in RPG pawn dialogue.
- Added: A hover tooltip that lists NPC potential RPG actions.
- Improved: RPG send button label now uses keyed localization (`RimChat_SendButton`) instead of hardcoded text.

0.3.44
- Added: API settings now include prompt output language controls (`Follow system` / `Custom language`).
- Added: New setting persistence fields `PromptLanguageFollowSystem` and `PromptLanguageOverride`.
- Added: Diplomacy and RPG hierarchical prompt builders now inject an output-language guidance block.
- Added: New CN/EN language keys for output-language UI labels, placeholder, and hint text.

0.3.43
- Improved: Dialogue compression now supports staged secondary rounds after turn 50, with per-pass mark increment and deferred rollover when mark exceeds configured cap.
- Added: Dialogue compression policy is now configurable from settings (enable switch, keep-recent turns, secondary trigger/window, mark cap, and summary controls).
- Fixed: RPG compact environment worldview stripping now matches section headers case-insensitively to reliably remove long worldview blocks in compact mode.

0.3.42
- Improved: Added compact RPG environment mode for same-faction manual dialogue, removing the long worldview paragraph from the injected environment block.
- Improved: Same-faction compact actor-state now applies to both self/interlocutor static profile sections (story/skills/equipment/genes/recent memories), reducing prompt payload further.
- Improved: RPG prompt assembly now uses a unified compact-context gate (`preferCompactContext`) to drive environment, actor-state, and API-contract compression together.

0.3.41
- Fixed: Deduplicated RPG `Recent Memories` entries before prompt injection to remove repeated labels and reduce token waste.
- Improved: Suppressed zero-state RPG psychological assessment injection when all five dimensions are neutral (all near 0), avoiding low-value prompt noise.
- Improved: Added compact RPG API contract mode for same-faction/manual dialogue to reduce prompt size while preserving action schema guidance.
- Improved: In same player-faction conversations, interlocutor status now uses a lighter static profile payload to reduce redundant context.

0.3.40
- Fixed: Removed duplicated player-faction line in RPG faction context injection (`AppendRPGFactionContext`).
- Fixed: Reduced redundant RPG prompt payload for same-player-faction conversations by avoiding duplicated colony inventory/alert blocks in interlocutor status.
- Improved: RPG hierarchical actor-state assembly now detects same player faction and applies shared-context deduplication.

0.3.39
- Changed: Replaced linear prompt assembly in `BuildFullSystemPrompt` and `BuildRPGFullSystemPrompt` with hierarchical context builders.
- Added: New hierarchical prompt model and renderer (`PromptHierarchyNode`, `PromptHierarchyRenderer`) to organize prompt data as structured sections.
- Added: New prompt setting `UseHierarchicalPromptFormat` (persisted in config and JSON import/export) to control XML-tag style rendering.
- Added: Prompts settings UI toggle and CN/EN language keys for hierarchical prompt format.
- Added: Prompt default config field `UseHierarchicalPromptFormat: true`.

0.3.38
- Added: Introduced `DialogueContextCompressionService` for dynamic prompt history packing in diplomacy/RPG channels.
- Added: New staged compression policy: keep latest 15 full turns, compress turns beyond 15, and apply a second compression pass to recency window 40-50 when total turns exceed 50.
- Added: Compression mark model (`+1` per pass, max `+2`) with deferred rolling behavior when a segment would exceed mark limit.
- Changed: Diplomacy main request path now uses compressed context instead of fixed recent-window truncation.
- Changed: Strategy follow-up request now reuses the same compressed context builder to reduce token spikes in long sessions.
- Changed: RPG request path now sends compressed conversation context while preserving all system prompts.

0.3.36
- Changed: Project-wide rebrand from RimDiplomacy to RimChat (solution/project/module folder names, assembly name, root namespace, class/type identifiers, language key prefixes, Def names, and packageId).
- Changed: Build/deploy scripts and workspace paths updated to RimChat; build output DLL renamed to RimChat.dll.
- Compatibility: This is a deep rename and may not be backward-compatible with old save/config identifiers that depended on previous names.

Version Update Log
0.3.35
- Changed: `ExitDialogueCooldown` in RPG dialogue now applies a 1-day cooldown (60000 ticks), and action-definition prompt text is aligned.
- Added: RPG memory fallback now performs one 80% roll after dialogue reaches 5 rounds; on success it auto-adds `TryGainMemory` (mood-affecting memory buff).
- Improved: RPG system feedback now uses a translucent panel and shows cooldown remaining time, memory-roll result, and memory-application details.
- Adjusted: Removed Recruit auto-completion logic and kept model-native action output only.
0.3.34
- Changed: RPG API action instructions now use a shared dynamic definition block at runtime, removing the legacy RPGApiGuidelines fixed-injection path.
- Improved: Removed the legacy "API Guidelines" editor section from RPG settings, and preview now matches the same dynamic action-definition block used at runtime.
0.3.33
- Changed: When RPG API is enabled, runtime system prompt now always injects the AppendRPGApiDefinitions action-definition block and no longer appends legacy RPGApiGuidelines text.
0.3.32
- Improved: RPG dialogue job now performs high-frequency moving-target checks during pathing (every 15 ticks) and repaths immediately when target position changes, instead of mostly waiting for stale-cell arrival retries.
- Improved: Unified the dialogue-open distance threshold constant to reduce mismatch-induced pauses between follow and window-open checks.
0.3.31
- Fixed: Completed LeaderMemory JSON persistence fields by adding read/write support for lastDecayCheckTick, playerRelationValues, and FactionMemoryEntry.firstContactTick/lastMentionedTick/relationHistory.
- Fixed: Removed the extra 50-entry trim during leader-memory file write to avoid over-truncation on top of the in-memory 200-entry cap.
- Added: RPG dialogue now persists per NPC into independent files at save_data/<saveName>/rpg_npc_dialogues/npc_<pawnId>.json.
- Added: NPC archives are hydrated back into RPG runtime state (persona prompt, RPG relation values, cooldown-until tick) after load for better continuity.
0.3.30
- Added: New-save startup now seeds baseline memory snapshots for all non-player factions (goodwill + current relation kind), preventing empty first-contact memory.
- Added: Post-load takeover now backfills legacy `FactionDialogueSession.messages` into faction memory, enabling mid-save adoption.
- Added: Idempotent marker events (`init-snapshot` / `session-backfill`) to prevent duplicate imports on repeated loads.
- Improved: Missing 5-dimension relation seeds are auto-recovered from current goodwill after load for better old-save compatibility.
0.3.29
- Added: Implemented bidirectional RPG-Diplomacy memory pipeline (RPG depart summary + diplomacy session-close summary) into faction long-term memory.
- Added: Harmony patch on Pawn.ExitMap(bool, Rot4) with strict guards (non-player faction, humanlike, player-home map, recent RPG trace).
- Added: Diplomacy window now generates exactly one session summary on close only when new messages were added.
- Added: RPG prompt now injects shared faction dynamic memory block without overwriting pawn persona (20 pool cap, 6 injected items, 2200-char budget).
- Added: Summary strategy is rule-first with low-confidence LLM fallback; if AI is unavailable it safely keeps rule summaries.
- Fixed: Corrected leader-memory JSON field mapping mismatch and added backward-compatible parsing for new cross-channel summary fields.
0.3.28
- Added: API settings now show "Recent dialogue token usage: xxxx (Low/Medium/High)" at the bottom.
- Added: Last dialogue token snapshot for Diplomacy and RPG channels (prompt/completion/total, channel, timestamp).
- Added: Usage parsing now prefers response usage fields, with fallback estimation from request+response text (4 chars 鈮?1 token) and estimated marker.
- Added: New CN/EN language keys RimChat_LastDialogueTokenUsage* and RimChat_TokenLevel*.
0.3.27
- Fixed: RPG dialogue job now refreshes moving-target alignment by looping back to follow until interaction distance is actually reached.
- Fixed: Added pre-open validation (same map/spawned/in-range) to prevent misaligned dialogue window triggers.
- Improved: Added reachability fail condition to reduce invalid executions when the target keeps moving.
0.3.26
- Fixed: RPG TryGainMemory now supports defName aliases and normalization (e.g., Chatted/Chat -> Chitchat) to prevent hard failures on non-standard names.
- Fixed: TryGainMemory failure feedback now includes valid defName examples for faster correction.
- Improved: RPG API guidance now provides a broader TryGainMemory defName set and explicitly states that Chatted is invalid (use Chitchat).
- Corrected: RPG default JSON template switched TryGainMemory example from invalid JoyFilled to valid Chitchat.
0.3.25
- Added: Player-faction pawn environment prompts can inject recent job state (current job, duty, and queued-job summary).
- Added: Player-faction pawn environment prompts can inject pawn attribute levels (core capacity percentages).
- Added: Environment prompt settings UI now includes IncludeRecentJobState / IncludeAttributeLevels toggles (enabled by default).
- Improved: Both diplomacy and RPG system prompts now inject FACT GROUNDING rules: reply only from known facts, and explicitly question unsupported claims.
- Compatibility: system_prompt_config.json and default seed now include the new fields with backward-compatible defaults.
0.3.24
- Fixed: RPG pawn context-menu Dialogue now queues a dialogue job first; the dialogue window opens only after the pawn reaches interaction range.
- Improved: Added a cooldown recheck at arrival time to prevent opening dialogue when cooldown state changes during travel.
0.3.23
- Added: Player-faction pawn environment prompts can inject a colony inventory summary (silver, food-days estimate, top stock items).
- Added: Player-faction pawn environment prompts can inject home-world alerts by reflecting active vanilla alerts dynamically (no hardcoded rules).
- Added: New RPG deep switches IncludeColonyInventorySummary and IncludeHomeAlerts, enabled by default.
- Improved: Environment prompt settings UI expands RPG switch area with the two new toggles and cleaner spacing.
- Compatibility: Legacy system_prompt_config.json files auto-fill the new fields with default values.
0.3.22
- Added: New persistent world-event ledger WorldEventLedgerComponent (Scribe), storing recent map events and aggregated raid battle reports.
- Added: Pawn.Kill patch integration for casualty aggregation (attacker deaths / defender deaths / defender downed peak).
- Added: request_raid / trigger_incident intent records to preserve AI action causality in long-term memory.
- Added: Environment injection block === RECENT WORLD EVENTS & BATTLE INTEL === shared by both diplomacy and RPG channels (manual + proactive).
- Added: EventIntelPrompt config set (channel toggles, memory window, storage cap, injected item cap, char cap), enabled by default.
- Improved: Prompts -> Environment Prompts UI adds Event Memory section and rebalanced layout to avoid overlap/misalignment.
- Changed: Environment injection order is now Worldview -> Environment Parameters -> Recent World Events & Battle Intel -> Scene Layers -> Existing Prompt Stack.
0.3.21
- Added: Environment parameter switches (time, date, season, weather, location+temperature, terrain, beauty, cleanliness, surroundings, map wealth) for per-item environment-layer injection.
- Added: Persistent EnvironmentContextSwitches config with default seed values and backward-compatible auto-fill for legacy configs.
- Improved: Prompts -> Environment Prompts UI rebuilt into grouped cards with a stable two-column layout to fix overlap and misalignment.
- Improved: Scene editor reorganized into a clearer vertical form (name, priority, channel toggles, tags, content) with improved preview readability.
- Changed: Environment-layer order is now Worldview -> Environment Parameters -> Scene Layers, then the existing prompt stack remains additive.
0.3.20
- Added: RimTalk-style Environment Prompt System with a global worldview layer and shared scene prompt library.
- Added: Shared scene entries now adapt across diplomacy/RPG channels and apply to both manual and proactive dialogue flows.
- Added: Per-scene switches (Enabled / ApplyToDiplomacy / ApplyToRPG) with ALL-tag matching and priority ordering.
- Added: Hard length caps (per-scene cap + total scene cap) to reduce instability from oversized prompts.
- Added: RPG deep pawn parameter toggles (skills, equipment, genes, needs, hediffs, recent memories).
- Added: New Environment Prompts section inside Prompts tab, including worldview editor, scene CRUD, toggles, and injection preview.
- Changed: Prompt assembly order is now Worldview -> Scene Layers -> Existing Prompt Stack, while keeping legacy prompt fields additive.
0.3.19
- Fixed: PawnRPG proactive letter title now uses "{sender} wants to talk with {receiver}" instead of "talk with you".
- Fixed: Opening RPG dialogue from a PawnRPG proactive letter no longer regenerates an opener; the proactive line is injected as the first NPC message.
0.3.18
- Fixed: PawnRPG force-debug trigger now bypasses relation threshold and master-feature gating, preventing no-op debug clicks.
- Changed: Added debug-only pairing path with bypassRelation while keeping normal runtime rules unchanged.
0.3.17
- Fixed: Added a PawnRPG proactive dialogue debug button in the RPG settings page (RPG Pawn Persona section), so debugging is no longer limited to the NPC proactive settings section.
- Changed: Updated PawnRPG debug-failure wording to remove the "non-player faction only" constraint and match current player-faction-internal proactive support.
0.3.16
- Added: PawnRPG proactive dialogue now supports player-faction internal pawn-to-pawn initiation (not limited to non-player faction initiators).
- Changed: Candidate faction scan now includes all valid humanlike pawn factions on player-home maps, including the player faction.
- Changed: Receiver selection widened from interactive colonists to valid player-faction humanlike pawns (excluding self).
- Compatibility: Legacy faction proactive channel remains unchanged; PawnRPG throttling/queue/failure policies remain unchanged.
0.3.15
- Added: Independent PawnRPG proactive dialogue channel, running in parallel with the legacy faction proactive channel (legacy behavior unchanged).
- Added: PawnRPG trigger trio (unconditional/conditional/causal), with task-category triggers bound to accepted Quest deadlines (<= 1 day remaining).
- Added: Causal ingress expanded to completed trades, major goodwill shifts, and threat-edge events (hostile units / nearby hives).
- Added: Relation threshold upgrade: intimate relations (Spouse/Fiance/Lover) bypass, otherwise Opinion >= 35; low-mood threshold set to Mood <= 0.30.
- Added: PawnRPG-specific throttling: per-NPC 6-day cooldown after successful delivery; non-warning global 3-day cooldown after successful delivery; warnings bypass only global cooldown.
- Added: Busy triple gate plus sleep/downed/working gate now feed into PawnRPG delayed queue (cap 3 per faction, 12h expiry).
- Added: PawnRPG proactive letter with one-click open into RPG dialogue window.
- Added: New settings debug button to force one PawnRPG proactive trigger (legacy debug button kept; new button bypasses gates for QA).
- Changed: PawnRPG LLM failure policy stays "retry once then drop", and cooldown counters update only on successful delivery.
0.3.14
- Changed: Removed the main-window entry and hid the bottom main button; Social Circle is now accessed from a tab in the diplomacy dialogue window.
- Added: Social post descriptions now include poster leader context, with effect-correlated narrative text.
- Added: Extended social impacts now support settlement gain/loss and native incidents (Cold Snap, Blight) under world-state constraints.
- Added: Post like interaction (default likes scale with faction settlement count; low chance to grant small goodwill bonus).
- Improved: Diplomacy dialogue faction list visuals with responsive goodwill value/relation effects.
- Added: Synchronized CN/EN language keys for all new social-circle and diplomacy-window UI text.
0.3.13
- Added: New "Social Circle" sub-tab in the main window with public feed, category filters, and unread count.
- Added: Social-circle runtime subsystem (models, save persistence, scheduler, soft impact, and intent scoring).
- Added: New AI action `publish_public_post` plus dialogue keyword fallback post generation.
- Added: Social-circle settings in Mod options (default interval 5-7 days, auto-action toggle, and related switches).
- Added: Always-visible "Force Generate Once" debug button in Mod settings.
- Improved: Action eligibility and prompt migration now include `publish_public_post`.
- Added: Synchronized Chinese/English language keys for all new social-circle UI and messages.
0.3.9
- Added: A debug button in NPC proactive dialogue settings to forcibly trigger one random proactive message.
- Added: Debug entry `GameComponent_NpcDialoguePushManager.DebugForceRandomProactiveDialogue()`.
- Improved: Debug trigger now shows success/failure feedback for non-playing state, no valid factions, and missing AI configuration.
0.3.8
- Added: NPC proactive dialogue subsystem with unconditional, conditional, and causal triggers, delivered through right-side letters.
- Added: Triple busy-gate checks (drafted colonists, hostile pawns, high click rate) and delayed queue handling.
- Added: Presence gate integration; triggers are queued while faction is offline or player is busy, then delivered by due time order.
- Added: Causal patches for low-quality weapon sales (Poor and below) and major goodwill shifts (|delta| >= 10).
- Added: Proactive letter type `ChoiceLetter_NpcInitiatedDialogue` with one-click open diplomacy dialogue action.
- Added: Config options (master toggle, frequency mode, queue cap/expiry, busy detection toggles) and synchronized CN/EN keys.
- Changed: Proactive messages always use LLM generation; one retry on failure, then drop with warning log.
0.3.7
- Added: RPG dialogue settings now support a per-colony-pawn persona prompt.
- Added: Per-pawn persona prompts are persisted per save via game component serialization.
- Improved: RPG system prompt assembly now injects the selected target pawn persona prompt automatically.
- Added: New UI text keys synchronized in both Chinese and English (no hardcoded new UI text).
0.3.6
- Added: Faction presence system (`Online/Offline/DoNotDisturb`) with per-faction persistence and cache metadata.
- Added: Per-faction 8-hour presence cache lock after closing the diplomacy dialogue window.
- Added: Night-time offline bias rule (time-slice schedule now computes only Online/Offline, excluding DND).
- Added: AI presence action protocol `exit_dialogue`, `go_offline`, and `set_dnd`.
- Added: Dialogue input gate (Offline/DND are read-only and cannot send); `exit_dialogue` now supports reinitiating dialogue.
- Added: Presence parameters are configurable in Mod settings (basic options + advanced tech-level templates).
- Added: New UI text keys are synchronized in both Chinese and English, with no hardcoded new UI strings.

0.3.4
- Added: Support for generating native-like quests using QuestScriptDef
- Added: AI can now specify questDefName and dynamically inject Slate parameters
- Optimized: Automatic resolution of Faction and Pawn parameters from AI strings
- Optimized: Auto-completion of mandatory parameters (e.g., map, points)

0.3.3
- Added: Support for LLM to create custom quests (CreateQuest) and trigger native events (TriggerIncident) via JSON instructions
- Added: Quest status callback system, supporting monitoring of player acceptance/rejection/completion/failure actions and feeding back to AI
- Added: Dynamic quest template, supporting AI injection of title, description, and reward content
- Improved: RPG API guidelines, added instruction descriptions for event triggering and quest creation

0.3.3
- Improved: Refactored custom quest system to fix unknown completion methods
- Improved: Introduced durationDays parameter for timer-based quest objectives
- Improved: Fixed redundant reward fields in the quest UI
- Fixed: Enhanced QuestPart_AICallback signal recognition for accurate AI feedback
- Fixed: Optimized QuestNode_AddAICallback signal resolution logic

0.3.2
- Refactored: RPG dialogue prompt settings UI with professional split layout (left navigation, right editor)
- Added: Collapsible live preview for RPG prompts with independent scrolling and dynamic content
- Improved: RPG API guidelines and JSON format constraint prompts to explicitly standardize AI output
- Fixed: Refined dynamic data injection for NPC status, player data, 5-dimension assessment, and faction background

0.3.1
- Added: RPG-style immersive dialogue interface with large portraits and bottom dialogue box layout
- Added: Right-click menu option on pawns to initiate RPG dialogue directly
- Added: Independent AI system prompt and dialogue template configuration for RPG mode
- Added: New "RPG Dialogue" tab in Mod settings for parameter adjustment
- Improved: Pawn portrait rendering now supports horizontal flipping

0.3.0
- Added: Asynchronous delayed diplomacy event system (Caravans, Aid, Raids)
- Added: Support for multiple caravan types (General, Bulk, Combat, Exotic, Slaver)
- Added: Support for multiple aid types (Military, Medical, Resources)
- Improved: AI action executor now handles delayed triggers
- Improved: Enhanced diplomacy notification manager for delayed events

0.2.9
- Added: "Royal Actions" card in diplomacy dialog (visible for Empire with titled pawn)
- Added: "Active Quests" card in diplomacy dialog (shows faction-related quests)
- Added: Quest card click navigates to vanilla quest details
- Improved: Comms console patch no longer intercepts "Permit", "Laborer" etc., preserving vanilla entry points

0.2.8
- Added: Orbital trader notification card at the top of diplomacy dialog
- Added: Click on the card to switch to vanilla trade dialog (requires available negotiator)
- Improved: Updated comms console callback logic to pass negotiator context
- Improved: Added relevant language keys

0.2.7
- Added: API actions now support adding new items (button at bottom of list)
- Added: API actions now support deletion (delete button in editor area with confirmation dialog)
- Added: Decision rules now support adding new items (button at bottom of list)
- Added: Decision rules now support deletion (delete button in editor area with confirmation dialog)
- Improved: Adjusted API actions and decision rules editor layout to reserve space for add/delete buttons
- Improved: All text editors (global prompt, API description, decision rules, JSON template) optimized for content height calculation
- Improved: Text area width reduced by scrollbar width to prevent horizontal scrollbar
- Improved: Text content height automatically calculated with 10px margin to ensure complete display without truncation
- Improved: Auto-select newly added items, auto-adjust selection index after deletion
- Improved: Complete language key support (Add, Delete, Confirm Title, Toast Messages, etc.)

0.2.6
- Added: Split "Response Format" section in Advanced Mode into three independent navigation items (JSON Response Template, Relation Changes Template, Important Rules)
- Improved: Each response format configuration can now be edited and viewed separately, improving interface clarity

0.2.5
- Improved: Fixed navigation options being pushed up, added independent scroll support
- Improved: Fixed preview box and text box not reaching bottom, using yMax to fill space
- Improved: All text editors (global prompt, API description, JSON template, rule content) fill available area
- Improved: Buttons fixed at bottom of editor area, don't scroll with content

0.2.4
- Improved: Fixed navigation panel and button area misalignment in prompt settings
- Improved: Integrated button area into navigation panel for continuous border
- Improved: Adjusted button spacing and added separator line for clearer visual

0.2.3
- Improved: Prompt settings bottom buttons changed to vertical layout, each button takes one row

0.2.2
- Improved: Prompt settings bottom four buttons moved to left navigation panel bottom
- Improved: Removed overall vertical scrollbar from prompt settings, changed to fixed single page layout

0.2.1
- Refactored: API actions and decision rules editor to left-right split layout (200px left list, right editor area)
- Fixed: Resolved text truncation issue in API call descriptions and decision rules
- Improved: List area scrolls independently, editor text area scrolls independently
- Improved: Friendly hint message displayed when no item is selected
- Improved: Text areas automatically calculate content height for complete scrolling
- Added: Language keys (Edit API Action, Edit Decision Rule, Name labels, selection hints, etc.)

0.2.0
- Improved: AI call description and decision rule text areas now have scrollbars to prevent text truncation
- Improved: Preview display moved to bottom of right editor area
- Improved: All text areas now support vertical scrolling (global prompt, API description, JSON template, rule content)
- Improved: Removed overall scrollbar from prompt settings, changed to fixed height single page layout
- Added: Complete language keys (simple/advanced mode short labels, preview short label, description/rule content labels, enable/disable buttons, etc.)
- Improved: Import/Export dialog UI, unified RimWorld native style dialogs

0.1.9
- Refactored: Prompt settings UI to left-right split layout with navigation and editor ratio 1:2.5
- Improved: All content limited to one page (480px fixed height), removed scrollbars
- Improved: Only one text editor displayed at a time, content switches based on left navigation
- Improved: Simple/Advanced mode toggle changed to small buttons at top (Simple/Advanced)
- Improved: Left navigation shows mode-related sections (2 for simple mode, 5 for advanced mode)
- Improved: Selected section has left blue accent bar and background highlight
- Improved: Preview function moved to bottom of left navigation panel (advanced mode only)

0.1.8
- Refactored: Prompt settings UI architecture using RimWorld native UI components
- Fixed: Eliminated nested scrollbars and horizontal scrollbar issues
- Fixed: Resolved UI text overlap and layout problems
- Improved: Text areas use native Widgets.TextArea with sufficient height (200px simple mode / 150px advanced mode)
- Improved: Mode toggle uses native button styling to avoid compatibility issues from custom drawing
- Improved: Advanced mode changed to vertical flow layout with horizontally arranged section buttons
- Improved: All editor areas use Listing_Standard for unified management, avoiding manual coordinate calculations
- Simplified: Removed complex split panels, drag-to-resize, nested ScrollViews and other complex interactions

0.1.7
- Improved: Prompt settings UI/UX for Simple and Advanced mode switching
- Improved: Mode toggle controls with card-style design, visual hierarchy and selection indicators
- Improved: Simple mode text area with adaptive height, real-time character count and color feedback
- Improved: Dynamic data injection options with card-style layout for better visibility
- Improved: Advanced mode left navigation panel with scroll support and accent color bars for selected items
- Improved: Editor area with header bar, preview area with status bar showing line and character counts
- Added: Gradient separator line effect for better visual separation
- Added: Related language key support (RimChat_SectionsLabel, etc.)

0.1.6
- Fixed: Five-dimension relationship progress bar value display error, corrected fill calculation logic for positive/negative values
- Added: Five-dimension relationship panel now supports collapse/expand functionality
- Added: Collapsed state displays relationship summary (Excellent/Good/Neutral/Tense/Hostile)
- Improved: Adaptive height when collapsed, providing more space for message area

0.1.5
- Refactored: AI message sending mechanism changed to asynchronous non-blocking approach
- Added: AIChatServiceAsync using Unity coroutines for true background processing

0.3.5
- Added: Unified action/quest eligibility service (`ApiActionEligibilityService`) for faction-scoped API filtering.
- Added: Dynamic prompt action injection filtering; `create_quest` is hidden from ACTIONS while on cooldown.
- Changed: `create_quest` now requires a valid `questDefName` template (strict mode).
- Changed: Rule-mismatched quests no longer redirect to `RimChat_AIQuest`.
- Changed: Technical quest generation errors no longer fallback; they fail directly and are reported via logs/system message flow.

0.3.10
- Added: Strategy suggestion buttons in diplomacy dialogue when the current round causes net goodwill decrease (fixed 3 buttons, click-to-send hidden reply).
- Added: Optional LLM JSON field `strategy_suggestions` with `short_label`, `trigger_basis`, `strategy_keywords`, and `hidden_reply`.
- Added: Strategy suggestions are shown only for the next send cycle and are cleared after any send.
- Improved: Five-dimension panel is now minimized to a single icon by default and expands as a compact floating overlay.
- Improved: Expanding/collapsing the five-dimension overlay no longer shifts strategy-button or input-bar layout.

0.3.11
- Fixed: When goodwill decreases but the first LLM payload omits `strategy_suggestions`, the client now issues one non-blocking follow-up request to recover the 3 strategy buttons.
- Improved: Five-dimension icon and strategy buttons are fixed on the same control row, and strategy buttons now animate in smoothly.
- Improved: Compact five-dimension overlay now has open/close transition animation without shifting strategy or input layout.
- Improved: Reinitiate-dialogue action was redesigned into a compact pill button with cleaner visual weight.

0.3.12
- Changed: Added a 1-hour cooldown before showing the reinitiate button after `exit_dialogue`; cooldown text is shown while waiting.
- Fixed: Resolved strategy-button click crash (`ArgumentOutOfRangeException`) caused by list mutation during same-frame render loop.
- Fixed: Placeholder outputs such as `I understand.` / `Your in-character response here` are no longer shown directly; parser now falls back to narrative text or default reply.
- Changed: Strategy ability moved from goodwill-drop-only trigger to session-use gating by Social skill: Social < 5 locked, 5-9 => 1 use, 10-14 => 2 uses, 15+ => 3 uses.
- Improved: Faster strategy-button reveal, stricter label display limit (short label only), and added appearance glow effect.
- Improved: Reduced strategy row border prominence and added tiny status hints for lock/remaining/generating states.
- Improved: Prompt guidance now emphasizes player-attribute-grounded strategies and shorter suggestion fields.


0.3.33
- Refactor: Removed legacy two-parameter prompt builder entries and unified both channels on `isProactive + additionalSceneTags` signatures.
- Fixed: Prompt previews in Diplomacy and RPG settings now call the final runtime prompt assembly path, so preview matches actual requests.
- Added: Manual-channel scene-tag settings (`DiplomacyManualSceneTagsCsv`, `RpgManualSceneTagsCsv`) for `additionalSceneTags` injection.
- Added: Preview-context controls (manual/proactive + scene tags) for validating both diplomacy and RPG workflows.
- Cleanup: Removed old hand-assembled preview text logic to avoid preview/runtime drift.

0.3.37
- Fixed: Prompt section navigation now preserves scroll position instead of resetting every draw.
- Fixed: API action list scroll and description editor scroll now use independent state.
- Added: Prompt variable reference dialog with click-to-insert `{{variable}}` tokens.
- Added: Variable validation for current editable section (unknown variable warning).
- Added: Environment scene prompt supports runtime `{{variable}}` rendering in prompt assembly.
- Added: Preview diagnostics now includes scene-layer match/skip reasons, truncation flags, and unknown variable hints.

0.3.46
- Added: Reflection-based `RimTalkCompatBridge` to bind `RimTalkPromptAPI` and `ScribanParser` without compile-time dependency.
- Added: Diplomacy session-close summary push to RimTalk global variables (`last` keys + rolling `recent` list).
- Added: RPG manual window-close session summary build/push without extra AI request.
- Added: Prompt compatibility template injection at stack tail for both diplomacy and RPG; rendered by RimTalk Scriban with safe fallback.
- Added: New settings `EnableRimTalkPromptCompat`, `RimTalkSummaryHistoryLimit`, `RimTalkCompatTemplate` and shared UI entry in RPG dynamic injection panel.
- Added: CN/EN language keys for RimTalk compatibility UI and variable documentation.
- Updated: Documentation sync for README/Api/config and bumped mod version to `0.3.46`.

0.3.47
- Added: RimTalk variable browser in RPG dynamic injection settings, including plugin/custom variables from RimTalk registry.
- Added: RimTalk prompt-entry creator in settings to add/update active preset entries (name/anchor/role/position/depth/content).
- Added: Bridge APIs `IsRuntimeAvailable`, `GetRegisteredVariablesSnapshot`, and `TryAddOrUpdateUserPromptEntry`.
- Added: Active RimTalk preset mod-entry render block injection into RPG prompt (`rimtalk_preset_mod_entries`).
- Improved: Delayed warmup path (`StartedNewGame`/`LoadedGame`/`FinalizeInit`) to improve runtime variable/compat-entry registration timing.
- Fixed: Restored split partial bridge members and models after refactor; build is green on RimWorld 1.6 / .NET 4.8.
- Fixed: Added a dedicated RPG settings section entry for RimTalk tools and an in-page jump button from dynamic injection, so variable browser/entry editor are always reachable.

0.3.48
- Changed: RimTalk compatibility tools now focus on reading RimTalk/plugin variables and injecting them into RimChat RPG prompt template.
- Changed: Removed manual RimTalk prompt-entry writer UI from RimChat settings to avoid editing RimTalk presets from the compat tool panel.
- Kept: Existing RimTalk variable/entry registration behavior in bridge runtime remains unchanged.

0.3.49
- Improved: AI Control tab now has a top quick-tools bar with live summary (`enabled actions` and `API limit`), section search, and one-click jump buttons.
- Improved: Accordion section headers are now card-styled with hover/expanded states and accent indicators for clearer scanability.
- Improved: AI tab scroll content height budget was adjusted to avoid clipping after the new quick-tools header.

0.3.61
- Refactor: Added prompt build orchestrator classes `DiplomacyPromptBuilder` and `RpgPromptBuilder`, and delegated `PromptPersistenceService` entry points to them without behavior change.
- Refactor: Added `PromptConfigStore` to centralize prompt config file existence/read/write operations.
- Internal API: Added hierarchical core wrappers `BuildFullSystemPromptHierarchicalCore` and `BuildRpgSystemPromptHierarchicalCore` for builder delegation.
- Docs: Updated README and Api module maps for prompt build decomposition.

0.3.62
- Added: `PromptConfigJsonCodec` typed JSON codec for `SystemPromptConfig` (serialize/deserialize + normalization).
- Changed: `PromptPersistenceService.SerializeConfigToJson(...)` now uses typed codec first, then legacy serializer fallback.
- Changed: `PromptPersistenceService.ParseJsonToConfigInternal(...)` now uses typed codec first, then legacy parser fallback.
- Docs: Updated README and Api with prompt JSON codec module notes.

0.3.63
- Fixed: Runtime `TypeLoadException` caused by `System.Web.Script.Serialization.JavaScriptSerializer` in RimWorld runtime.
- Changed: `PromptConfigJsonCodec` now uses `UnityEngine.JsonUtility` for typed prompt-config JSON encode/decode.
- Changed: Added `[Serializable]` to `SystemPromptConfig` related config models and `EventIntelPromptConfig`.
- Changed: Removed `System.Web.Extensions` reference and added `UnityEngine.JSONSerializeModule` project reference.






