版本更新日志
0.3.21
- 新增：环境参数开关（时间、日期、季节、天气、位置与温度、地形、美观度、清洁度、周边环境、地图财富），支持按项注入到环境层提示词。
- 新增：EnvironmentContextSwitches 配置持久化与默认种子，兼容旧配置自动补全。
- 优化：Prompts -> 环境提示词 UI 重构为分组卡片与稳定两栏布局，修复原有错乱/挤压问题。
- 优化：场景编辑器改为纵向清晰表单（名称、优先级、通道开关、标签、内容），并增强预览区可读性。
- 变更：环境层注入结构扩展为“世界观 -> 环境参数 -> 场景层”，并继续叠加旧提示词体系。
0.3.20
- 新增：RimTalk 风格环境提示词系统，支持“世界观全局提示词 + 场景提示词库”。
- 新增：场景条目共用库支持外交/RPG 双通道自适应，覆盖手动会话与主动推送链路。
- 新增：场景条目级开关（启用、外交通道、RPG 通道）与优先级/标签匹配（ALL 命中）机制。
- 新增：长度硬上限控制（单条上限 + 总量上限），降低长提示词导致的不稳定风险。
- 新增：RPG 深度 Pawn 参数开关（技能、装备、基因、needs、hediff、近期记忆）。
- 新增：Prompts 页内“环境提示词”分区 UI，含世界观编辑、场景 CRUD、参数开关与注入预览。
- 变更：提示词组装顺序调整为“世界观 -> 场景 -> 现有提示词体系”，并保持旧配置字段叠加生效。
0.3.19
- 修复：PawnRPG 主动通知标题改为“{发起者} 想和 {目标} 聊聊”，不再使用“想和你聊聊”。
- 修复：从 PawnRPG 主动信件打开 RPG 对话时，不再重新生成开场；主动消息直接作为会话首句。
0.3.18
- 修复：PawnRPG 强制调试触发改为绕过关系阈值与总开关门控，避免点击调试按钮无响应。
- 变更：调试路径专用配对逻辑新增 bypassRelation，常规路径规则不变。
0.3.17
- 修复：RPG 对话设置页（RPG Pawn 独立人格分区）补充 PawnRPG 主动对话调试按钮，避免仅在 NPC 主动设置分区可触发。
- 变更：PawnRPG 调试失败提示文案移除“仅非玩家派系”限制描述，匹配当前支持玩家派系内部主动对话的规则。
0.3.16
- 新增：PawnRPG 主动对话支持玩家派系内部 Pawn 对 Pawn 发起，不再局限于非玩家派系发起。
- 变更：主动对话候选扫描扩展为玩家家园地图全部有效人形 Pawn 派系（含玩家派系）。
- 变更：目标 Pawn 选择范围从“可交互殖民者”扩展为玩家派系有效人形 Pawn（排除自身）。
- 兼容：旧派系主动通道行为保持不变，PawnRPG 独立节流/队列/失败策略不变。
0.3.15
- 新增：PawnRPG 主动对话独立通道（与旧派系主动外交通道并行，旧通道行为不变）。
- 新增：PawnRPG 三类触发（无条件/条件/因果），其中任务类绑定已接受 Quest 截止（剩余 <= 1 天）。
- 新增：因果入口扩展到交易完成、好感大幅波动、威胁态（敌对单位/虫巢）边沿触发。
- 新增：关系筛选与阈值规则：亲密关系（配偶/未婚/恋人）直通，否则 Opinion >= 35；低心情阈值为 Mood <= 0.30。
- 新增：PawnRPG 专用节流规则：每 NPC 成功投递后 6 天冷却；非警告类全局成功投递后 3 天冷却；警告类仅绕过全局冷却。
- 新增：忙碌三重门控与睡觉/昏迷/工作中门控统一接入 PawnRPG 延迟队列（每派系 3 条、12 小时过期）。
- 新增：PawnRPG 主动信件与一键打开 RPG 对话窗口。
- 新增：设置页调试按钮“强制触发 PawnRPG 主动对话”（保留旧按钮；新按钮绕过门控用于联调）。
- 变更：LLM 失败策略在 PawnRPG 通道保持“重试 1 次后丢弃”，且冷却计数仅按成功投递更新。
0.3.14
- 变更：移除主窗口入口，隐藏底部按钮；社交圈入口迁移到外交对话窗口页签。
- 新增：社交帖子描述注入发帖派系领袖信息，并按影响类型生成关联叙事文案。
- 新增：社交圈扩展影响支持定居点新增/丢失与原生事件触发（寒潮、枯萎病），并带世界状态约束。
- 新增：社交圈帖子点赞交互（默认点赞数按派系定居点规模生成，低概率奖励少量好感度）。
- 优化：外交对话窗口派系列表视觉重绘，好感度数值与关系标签加入响应式动态效果。
- 新增：中英文语言键同步扩充，新增社交圈与外交窗口 UI 文案全部走键值。
0.3.13
- 新增：主窗口加入“社交圈”子标签，支持公开公告流、类别筛选与未读计数。
- 新增：社交圈系统（数据模型、存档持久化、周期调度、软影响与意图分）。
- 新增：AI 动作 `publish_public_post`，并支持对话关键词兜底生成公告。
- 新增：Mod 设置增加社交圈配置项（默认间隔 5~7 天、自动执行开关等）。
- 新增：Mod 设置页增加“强制生成一次”调试按钮（始终显示）。
- 优化：动作资格校验与提示词迁移补齐 `publish_public_post`。
- 新增：中英文语言键同步更新，社交圈 UI 与提示文案全面去硬编码。
0.3.9
- 新增：NPC 主动对话设置增加调试按钮，可手动强制触发一条随机主动对话。
- 新增：`GameComponent_NpcDialoguePushManager.DebugForceRandomProactiveDialogue()` 调试入口。
- 优化：调试触发后显示成功/失败提示，失败原因包含“非游戏中/无可用派系/AI 未配置”等场景。
0.3.8
- 新增：NPC 主动对话子系统，支持无条件/条件/因果三类触发并统一走右侧信件投递。
- 新增：忙碌检测三重门控（征召状态、敌对单位、高频点击）与延迟队列机制。
- 新增：在线状态门控，派系离线或玩家忙碌时触发入队，恢复可用后按到期顺序投递。
- 新增：因果触发 Patch：交易后低品质武器（Poor 及以下）与大幅好感变动（|delta| >= 10）。
- 新增：主动对话专用信件 `ChoiceLetter_NpcInitiatedDialogue`，支持一键打开外交对话。
- 新增：配置项（总开关、频率档位、队列上限/过期、忙碌检测开关）与中英文语言键。
- 变更：主动消息强制走 LLM 生成，失败重试 1 次后丢弃并写日志。
0.3.7
- 新增：RPG 对话设置支持为每个殖民地 Pawn 配置独立人格 Prompt。
- 新增：殖民地 Pawn 人格 Prompt 按存档持久化（随游戏存档读写）。
- 优化：RPG 系统提示词构建时自动注入目标 Pawn 的独立人格 Prompt（若已配置）。
- 新增：中英文语言键同步更新，避免新 UI 文案硬编码。
0.3.6
- 新增：派系在线状态系统（在线/离线/请勿打扰），支持按派系持久化与缓存。
- 新增：外交窗口关闭后按派系进入 8 小时在线状态缓存锁。
- 新增：夜间偏离线规则（在线分时段仅计算在线/离线，不含请勿打扰）。
- 新增：AI 动作协议 `exit_dialogue`、`go_offline`、`set_dnd`。
- 新增：对话输入门控（离线/请勿打扰只读，不允许发送）；`exit_dialogue` 支持“重新发起对话”。
- 新增：在线状态参数可在 Mod 设置中调整（基础项 + 高级科技等级模板）。
- 新增：中英文语言键同步更新，移除新增功能中的硬编码文案。

0.3.4
- 新增：支持套用原版任务模板 (QuestScriptDef) 的生成机制
- 新增：AI 现在可以指定 questDefName 并动态注入 Slate 参数
- 优化：自动解析 AI 传入的派系 (Faction) 和人物 (Pawn) 参数
- 优化：支持任务参数自动补全 (如 map, points)

0.3.3
- 新增：支持 LLM 通过 JSON 指令创建自定义任务 (CreateQuest) 和触发原生事件 (TriggerIncident)
- 新增：任务状态回调系统，支持监听玩家接受/拒绝/完成/失败动作并反馈给 AI
- 新增：动态任务模板，支持 AI 注入标题、描述和奖励内容
- 优化：RPG API 指南，添加事件触发和任务创建的相关指令说明

0.3.3
- 优化：重构自定义任务系统，修复任务完成方式不明的问题
- 优化：引入任务时长 (durationDays) 参数，支持计时器型任务目标
- 优化：修复 QuestNode_Delay 初始化时字段名称错误的 Bug (outSignalsCompleted -> outSignalComplete)
- 优化：修复任务界面奖励字段重复显示的缺陷
- 修复：完善 QuestPart_AICallback 信号识别逻辑，确保状态正确反馈给 AI
- 修复：优化 QuestNode_AddAICallback 对动态信号的解析流程

0.3.2
- 重构：RPG 对话提示词设置界面，采用专业左右分栏布局（左侧导航，右侧编辑）
- 新增：RPG 提示词实时折叠预览功能，支持独立滚动和动态内容展示
- 优化：RPG API 指南和 JSON 格式约束提示词，显式规范 AI 的 JSON 输出结构
- 修复：完善 NPC 状态、玩家数据、五维心理评估和派系背景的动态注入逻辑

0.3.1
- 新增：RPG 式沉浸对话界面，支持大立绘和底部对话框布局
- 新增：右键人物菜单直接发起 RPG 对话
- 新增：RPG 模式独立的 AI 系统提示词和对话模板配置
- 新增：Mod 设置中新增"RPG对话"选项卡，支持相关参数调整
- 优化：人物立绘渲染支持水平翻转处理

0.3.0
- 新增：异步延迟外交事件系统（商队、援助、袭击）
- 新增：支持多种商队类型（通用、大宗、战斗、异域、奴隶）
- 新增：支持多种援助类型（军事、医疗、资源）
- 优化：AI 动作执行器支持处理延迟触发的事件
- 优化：完善外交通知管理系统，支持延迟事件通知

0.2.9
- 新增：外交对话框中新增"帝国特权"卡片（仅对帝国派系且有爵位者显示）
- 新增：外交对话框中新增"相关任务"卡片（显示当前派系相关任务）
- 新增：任务卡片点击可直接跳转到原版任务详情页
- 优化：通讯台拦截逻辑，不再拦截"许可"、"劳工"等相关选项，保留原版功能入口

0.2.8
- 新增：在外交对话框顶部显示轨道商船通知卡片
- 新增：点击商船卡片可直接切换到原版交易界面（需有可用谈判者）
- 优化：修改通讯台回调逻辑，支持传递谈判者上下文
- 优化：添加相关语言键支持

0.2.7
- 新增：API 调用说明支持新增功能（列表底部添加按钮）
- 新增：API 调用说明支持删除功能（编辑区删除按钮，带二次确认）
- 新增：决策规则支持新增功能（列表底部添加按钮）
- 新增：决策规则支持删除功能（编辑区删除按钮，带二次确认）
- 优化：调整 API 调用说明和决策规则编辑区域布局，为新增/删除按钮预留空间
- 优化：所有文本编辑框（全局提示词、API 描述、决策规则、JSON 模板）优化内容高度计算
- 优化：文本框宽度减去滚动条宽度，避免水平滚动条出现
- 优化：文本内容高度自动计算并增加 10px 余量，确保内容完整显示无截断
- 优化：新增操作后自动选中新项目，删除操作后自动调整选中索引
- 优化：添加完整的语言键支持（新增、删除、确认标题、提示信息等）

0.2.6
- 新增：将高级模式"响应格式"分区拆分为三个独立导航项（JSON 响应模板、关系变化模板、重要规则）
- 优化：每个响应格式配置项现在可以单独编辑和查看，提升界面清晰度

0.2.5
- 优化：修复导航栏选项被顶上去的问题，添加独立滚动支持
- 优化：修复预览框和文本框没有到底的问题，使用 yMax 计算填满空间
- 优化：所有文本编辑框（全局提示词、API 描述、JSON 模板、规则内容）填满可用区域
- 优化：按钮固定在编辑区域底部，不随内容滚动

0.2.4
- 优化：修复提示词界面导航栏与按钮区域的错位问题
- 优化：将按钮区域整合到导航栏内部，保持边框连续
- 优化：调整按钮间距，增加分隔线，视觉更清晰

0.2.3
- 优化：提示词界面底部按钮改为纵向排列，每个占一行

0.2.2
- 优化：提示词界面底部四个按钮移至左侧导航栏底部
- 优化：移除提示词界面整体垂直滚动条，改为固定单页布局

0.2.1
- 重构：AI 调用说明和决策规则编辑区域改为左右分栏布局（左侧列表 200px，右侧编辑区）
- 修复：解决 AI 调用说明和决策规则文本被截断的问题
- 优化：列表区域独立滚动，编辑区域文本框独立滚动
- 优化：未选中任何项时显示友好的提示信息
- 优化：文本框自动计算内容高度，支持完整内容滚动显示
- 新增：语言键支持（编辑 API 调用说明、编辑决策规则、名称标签、选择提示等）

0.2.0
- 优化：AI 调用说明和决策规则文本框加入滚动条，解决文本截断问题
- 优化：预览显示移至右侧编辑区下方
- 优化：文本框全部加入上下滚动条支持（全局提示词、API 描述、JSON 模板、规则内容）
- 优化：去掉提示词界面整体滚动条，改为固定高度单页布局
- 新增：补全语言键（简单/高级模式短标签、预览短标签、描述/规则内容标签、启用/禁用按钮等）
- 优化：导入导出界面 UI，统一使用 RimWorld 原生风格对话框

0.1.9
- 重构：提示词设置界面改为左右分栏布局，导航栏与编辑区比例 1:2.5
- 优化：所有内容限制在一页中（480px 固定高度），去除滚动条
- 优化：一次只展示一个文本编辑框，根据左侧导航切换显示内容
- 优化：简单/高级模式切换改为顶部小按钮（Simple/Advanced）
- 优化：左侧导航栏显示模式相关分区（简单模式 2 个，高级模式 5 个）
- 优化：选中分区带左侧蓝色强调条和背景高亮
- 优化：预览功能移至左侧导航栏底部（仅高级模式）

0.1.8
- 重构：提示词设置界面 UI 架构，使用 RimWorld 原生 UI 组件
- 修复：消除嵌套滚动条和水平滚动条问题
- 修复：解决 UI 文字重叠和布局混乱问题
- 优化：文本框使用原生 Widgets.TextArea，高度足够大（200px 简单模式/150px 高级模式）
- 优化：模式切换使用原生按钮样式，避免自定义绘制导致的兼容性问题
- 优化：高级模式改为垂直流式布局，分区选择按钮水平排列
- 优化：所有编辑区域使用 Listing_Standard 统一管理，避免手动坐标计算
- 简化：移除复杂的分割面板、拖拽调整大小、嵌套 ScrollView 等复杂交互

0.1.7
- 优化：提示词设置界面简单模式与高级模式的 UI/UX 交互体验
- 优化：模式切换控件采用卡片式设计，增加视觉层次和选中状态指示器
- 优化：简单模式文本框自适应高度，增加实时字数统计和颜色反馈
- 优化：动态数据注入选项采用卡片式布局，提升可识别性
- 优化：高级模式左侧导航面板增加滚动支持，选中项带左侧强调色条
- 优化：编辑区域增加标题栏，预览区域增加状态栏显示行数和字符数
- 新增：渐变分隔线效果，提升视觉分隔感
- 新增：相关语言键支持（RimDiplomacy_SectionsLabel 等）

0.1.6
- 修复：五维关系进度条数值显示错误，修正正负值的填充计算逻辑
- 新增：五维关系维度界面支持折叠/展开功能
- 新增：折叠状态显示关系概要（关系极佳/良好/一般/紧张/恶劣）
- 优化：折叠时界面高度自适应，为消息区域留出更多空间

0.1.5
- 重构：AI 消息发送机制改为异步非阻塞方式
- 新增：AIChatServiceAsync 使用 Unity 协程实现真正的后台处理

0.3.5
- 新增：统一 API 过滤与任务模板校验服务（ApiActionEligibilityService），按派系动态判定可用 action。
- 新增：Prompt 动态注入过滤，冷却中的 create_quest 不再注入 ACTIONS，并显示拦截原因。
- 变更：create_quest 改为严格模板模式，缺失 questDefName 或模板不合法时直接失败。
- 变更：任务规则不匹配不再重定向到 RimDiplomacy_AIQuest。
- 变更：任务生成技术异常不再 fallback，直接失败并写入日志/系统消息链路。

0.3.10
- 新增：外交窗口支持降好感回合的策略建议按钮（固定 3 个，点击即发送隐藏回复）。
- 新增：LLM 可选 JSON 字段 `strategy_suggestions`（`short_label`、`trigger_basis`、`strategy_keywords`、`hidden_reply`）。
- 新增：策略建议仅在本轮净好感度下降后展示，并在任一发送后立即清空。
- 优化：五维关系区改为默认单图标入口，点击后以紧凑浮层展开，不再挤压底部 UI。
- 优化：五维浮层展开/收起不影响策略按钮与输入框布局位置。

0.3.11
- 修复：当本轮净降好感但 LLM 未返回 `strategy_suggestions` 时，自动发起一次补充策略请求，恢复策略按钮出现链路。
- 优化：关系维度图标与策略按钮固定同一行，策略按钮加入平滑出现动画。
- 优化：五维紧凑浮层加入展开/收起过渡动画，且不改变输入区与策略条布局。
- 优化：“重新发起对话”按钮改为紧凑胶囊样式，降低占用并提升可读性。

0.3.12
- 调整：`exit_dialogue` 结束后“重新发起对话”增加 1 小时冷却，冷却未到时显示提示文本，不显示按钮。
- 修复：策略按钮点击导致 `ArgumentOutOfRangeException` 的崩溃（点击后清空列表与同帧遍历冲突）。
- 修复：AI 占位文本 `I understand.` / `Your in-character response here` 不再直接显示，优先回退到正文或默认回复。
- 改动：策略能力从“仅降好感触发”改为“按会话次数与社交等级解锁”：社交<5 不可用；5-9 可用1次；10-14 可用2次；15+ 可用3次。
- 优化：策略按钮动画提速、文案显示限制（按钮仅显示短标题，超长截断）、加入出现发光特效。
- 优化：策略区边框视觉弱化，低社交/剩余次数/生成中状态以小字提示。
- 优化：提示词强化“基于玩家属性生成策略”约束，并降低按钮字段长度上限。





















